<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.health.assessment.mapper.UserMapper">

    <!-- 基础结果映射 -->
    <resultMap id="userResultMap" type="com.health.assessment.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="real_name" property="realName"/>
        <result column="age" property="age"/>
        <result column="gender" property="gender"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="bio" property="bio"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="deleted_at" property="deletedAt"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <!-- 查询所有活跃用户（分页） -->
    <select id="selectAllActiveUsers" resultMap="userResultMap">
        SELECT * FROM t_user
        WHERE status = 'ACTIVE' AND is_deleted = 0
        ORDER BY created_at DESC
    </select>

    <!-- 根据用户名模糊查询用户 -->
    <select id="selectByUsernamePattern" resultMap="userResultMap">
        SELECT * FROM t_user
        WHERE username LIKE CONCAT('%', #{username}, '%')
        AND is_deleted = 0
        ORDER BY created_at DESC
    </select>

    <!-- 根据条件查询用户 -->
    <select id="selectByCondition" resultMap="userResultMap">
        SELECT * FROM t_user
        <where>
            AND is_deleted = 0
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="email != null and email != ''">
                AND email = #{email}
            </if>
            <if test="phone != null and phone != ''">
                AND phone = #{phone}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="gender != null">
                AND gender = #{gender}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <!-- 统计用户数量 -->
    <select id="countUsers" resultType="int">
        SELECT COUNT(*) FROM t_user
        WHERE is_deleted = 0
        AND status = 'ACTIVE'
    </select>

    <!-- 统计特定时间内的新用户 -->
    <select id="countNewUsersSince" resultType="int">
        SELECT COUNT(*) FROM t_user
        WHERE is_deleted = 0
        AND created_at >= #{since}
    </select>

</mapper>

