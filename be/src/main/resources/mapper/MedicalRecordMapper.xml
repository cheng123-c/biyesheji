<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.health.assessment.mapper.MedicalRecordMapper">

    <!-- 基础结果映射 -->
    <resultMap id="medicalRecordResultMap" type="com.health.assessment.entity.MedicalRecord">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="record_type" property="recordType"/>
        <result column="record_title" property="recordTitle"/>
        <result column="record_content" property="recordContent"/>
        <result column="record_date" property="recordDate"/>
        <result column="hospital" property="hospital"/>
        <result column="doctor_name" property="doctorName"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- 分页查询用户医疗记录 -->
    <select id="selectByUserIdWithPage" resultMap="medicalRecordResultMap">
        SELECT * FROM t_medical_record
        WHERE user_id = #{userId}
        <if test="recordType != null and recordType != ''">
            AND record_type = #{recordType}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (record_title LIKE CONCAT('%', #{keyword}, '%')
                OR record_content LIKE CONCAT('%', #{keyword}, '%')
                OR hospital LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY record_date DESC, created_at DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 统计用户医疗记录数量 -->
    <select id="countByUserIdWithCondition" resultType="int">
        SELECT COUNT(*) FROM t_medical_record
        WHERE user_id = #{userId}
        <if test="recordType != null and recordType != ''">
            AND record_type = #{recordType}
        </if>
        <if test="keyword != null and keyword != ''">
            AND (record_title LIKE CONCAT('%', #{keyword}, '%')
                OR record_content LIKE CONCAT('%', #{keyword}, '%')
                OR hospital LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </select>

    <!-- 按类型统计数量 -->
    <select id="countByType" resultType="map">
        SELECT
            record_type,
            COUNT(*) as count
        FROM t_medical_record
        WHERE user_id = #{userId}
        GROUP BY record_type
    </select>

    <!-- 查询最近的医疗记录 -->
    <select id="selectRecentByUserId" resultMap="medicalRecordResultMap">
        SELECT * FROM t_medical_record
        WHERE user_id = #{userId}
        ORDER BY record_date DESC, created_at DESC
        LIMIT #{limit}
    </select>

</mapper>

