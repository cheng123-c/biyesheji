# TODO — 未实现功能

> 从README.md（来自开题报告）中，提取出的功能中，尚未实现或不完整的功能。
> 

---

## 一、用户管理模块


- [ ] **忘记密码 / 重置密码**（超管重置密码）——只有登录后的"修改密码"，未做
- [ ] **角色权限管理**——未做

---

## 二、数据采集模块

- [ ] **数据异常自动检测**——`t_data_anomaly` 表已建，未实现功能

---

## 三、AI 评估引擎

- [ ] **AI 模型管理**——`t_ai_model` 表已建但为空，后台无任何 AI 模型版本管理的接口和界面

---

## 四、系统管理后台

- [ ] **数据源管理页面**——`t_data_source` 表已建，但无后台增删改查接口和前端界面
- [ ] **AI 模型版本管理页面**——`t_ai_model` 表已建，但无管理接口和前端界面
- [ ] **报告模板管理**——`t_report_template` 表已建，但无接口和前端界面；PDF 样式当前写死在代码中
- [ ] **RBAC 权限可视化配置**——`t_role`、`t_permission`、`t_role_permission` 表已建，但后台无角色权限配置界面，权限由代码硬编码控制

---

## 五、知识图谱模块

- [ ] **知识图谱关系管理 UI**——`adminCreateRelation` / `adminDeleteRelation` 接口已写，`knowledgeApi.js` 也已封装，但管理后台的知识图谱 Tab 中无"关系"增删界面，只能通过直接写 SQL 建立节点关联

---

## 六、干预与服务生态模块

- [ ] **在线咨询 / 预约服务**——完全未做
- [ ] **健康商品推荐**  完全没做
- [ ] **干预方案效果量化追踪**——只能手动改状态，无效果量化评估逻辑
- [ ] **健康内容全文检索**——`t_health_content` 已建全文索引，`HealthContentMapper` 中也写了 `searchByKeyword`，但前端实际只走 LIKE 模糊查询，全文索引未正确触发

---

## 七、通知系统

- [ ] **数据异常自动推送通知**——上传健康数据时未触发异常检测和通知逻辑
- [ ] **复诊提醒**——完全未做

---

